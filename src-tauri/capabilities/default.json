{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Minimal permissions shared by the inbox and capture windows.",
  "windows": ["inbox", "capture"],
  "permissions": ["core:default"]
}

