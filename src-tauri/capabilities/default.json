{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Minimal permissions shared by the main, capture, and focus windows.",
  "windows": ["inbox", "capture", "focus"],
  "permissions": ["core:default"]
}
